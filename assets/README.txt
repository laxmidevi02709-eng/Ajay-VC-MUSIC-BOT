Place a TrueType font file named `font.ttf` here for the Now-Playing
thumbnail text. Recommended: download "Poppins-Bold.ttf" from Google Fonts
and rename it to `font.ttf`.

If no font.ttf is present, PIL falls back to its default font (still works,
just less stylish).
